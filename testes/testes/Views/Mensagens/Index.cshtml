@model IEnumerable<testes.Models.Mensagem>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MensagemConteudo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MensagemConteudo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MensagemID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MensagemID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MensagemID })
        </td>
    </tr>
}

</table>
